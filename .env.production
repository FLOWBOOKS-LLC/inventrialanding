# Loaded automatically by `vite build`. Safe to commit — this is your public Calendly page.
# Override locally with .env.production.local (gitignored) if needed.
VITE_CALENDLY_URL=https://calendly.com/flowbooksng/flowbooks
